/**
 * What to call this build out loud.
 *
 * Two numbers, because they answer different questions. VERSION is for people —
 * it goes up when something ships and it is what you quote back at me. The build
 * id is for machines: sw.js hashes the change-tag of every shipped file into
 * sixteen hex characters, so it changes when any byte does and it cannot be
 * forgotten the way a hand-bumped number can. If the version on your screen
 * matches mine and the build id does not, your browser is serving you a stale
 * shell — which is the whole reason both are on the screen.
 *
 * Bump VERSION and RELEASED when you ship something worth naming.
 */
export const VERSION = '1.6.0';
export const RELEASED = '2026-08-17';
